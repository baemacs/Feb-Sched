# Configuration files with sensitive data
config.js
# Note: config.js is generated by Netlify during build from environment variables
# Keep it locally for development but don't commit

# Build files
netlify.toml
# Note: netlify.toml is deployment config, can be committed

# Other common ignores
node_modules/
.DS_Store
*.log